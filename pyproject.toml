# SPDX-FileCopyrightText: 2025-2026 Tyrone Ross, Jr <46267523+tyroneross@users.noreply.github.com>
# SPDX-License-Identifier: Apache-2.0
[project]
name = "agent-doe-engine"
version = "0.2.0"
description = "A design-of-experiments engine for tuning AI agents: DOE matrix, multi-response effects, scalarization / desirability / Pareto selection, autoresearch fallback."
authors = [{ name = "Tyrone Ross, Jr", email = "46267523+tyroneross@users.noreply.github.com" }]
license = "Apache-2.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project.urls]
Homepage = "https://github.com/tyroneross/agent-doe-engine"
Repository = "https://github.com/tyroneross/agent-doe-engine"

[project.scripts]
agent-doe-engine-doe = "doe:main"
agent-doe-engine-suggest-factors = "suggest_factors:main"
agent-doe-engine-validate-factors = "validate_factors:main"
agent-doe-engine-worktree = "worktree:main"

[tool.setuptools]
py-modules = [
  "doe",
  "loop",
  "metric_runner",
  "objectives",
  "suggest_factors",
  "validate_factors",
  "worktree"
]

[tool.setuptools.package-dir]
"" = "scripts"

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
